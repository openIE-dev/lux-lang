# counter

A reactive counter with signal-driven state.

```bash
openie-lux build --target wasm Counter.lux
```

What this shows:
- `signal()` for reactive state
- `on:click` event handler
- `.text()` reactive text projection

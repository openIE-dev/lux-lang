# hello

The minimum Lux component: a function that takes a string and renders an `h1`.

```bash
openie-lux run Hello.lux
```

What this shows:
- The `component` declaration form
- A `view:` block
- String concatenation in a view expression
